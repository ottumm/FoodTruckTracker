#! /bin/bash

if [ ! -f lockfile ]
	then
		date > lockfile
		cd app
		ruby twitter_to_ical.rb -c food_trucks.json -o temp.ics
		ruby validate_ical.rb temp.ics && mv temp.ics /var/www/html/emeryville_food_trucks.ics && cp -f log.txt /var/www/html/log.txt
		date
		cd ..
		rm lockfile
fi

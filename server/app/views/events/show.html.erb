<p id="notice"><%= notice %></p>

<div class="container">
  <%= render :partial => "shared/event", :locals => { :event => @event, :show_corrections => true } %>
</div>

<%= link_to 'Back', events_path %>
